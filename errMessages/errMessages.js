const userNotFound = { message: "Usuario no encontrado" };

const companyNotFound = { message: "Compañia no encontrada" };

const incorrectData = {message: "Datos incorrectos o faltantes"}
module.exports = {
  userNotFound,
  companyNotFound,
  incorrectData
};
